1. 중복제거
 - distinct

 - group by
	-> 데이터를 그룹핑해서 그 결과를 가져오는
	경우 사용한다. 집계함수와 짝을 이루어
	사용할 수 있다.

 - having
	-> where절에서는 집계함수를 사용 할 수 없다.
	-> having절은 집계함수를 가지고 조건비교를
	할때 사용한다.
	-> having절은 group by 절과 함께 사용이 된다.

~3시








